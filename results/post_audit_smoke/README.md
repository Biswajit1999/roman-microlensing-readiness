# Software smoke output

These eight synthetic trials use `configs/smoke_test.yaml` and verify the blind
search, parameter-recovery fields, and output provenance. They are not a Roman
population, completeness, yield, or readiness result. The manifest records a
dirty working tree because the remediation had not yet been committed.
